{% extends 'base.html.twig' %}

{% block title %}{{ 'accounts.not_found.title'|trans }} · {{ 'app.name'|trans }}{% endblock %}

{% block body %}
    <section class="section">
        <div class="card">
            <h1 class="page-title">{{ 'accounts.not_found.title'|trans }}</h1>
            <p class="lead">{{ 'accounts.not_found.message'|trans }}</p>
            <a class="button button--primary" href="{{ path('app_account_list') }}">{{ 'accounts.list.heading'|trans }}</a>
        </div>
    </section>
{% endblock %}
